{
  "homepage": "https://joannalaw.github.io/dogether-todo",
  "name": "dogether-todo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "0.9.5"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "ignore-jekyll": "cp .nojekyll build/",
    "predeploy": "npm run build && npm run ignore-jekyll",
    "deploy": "gh-pages -d build --dotfiles",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
